<h1>Users#show</h1>
<p>Find me in app/views/users/show.html.erb</p>

<%= render "shared/search" %>

<div class="container">
  <div class="row">
  <% @games.sort_by { |el| el[:start] }.each do |game| %>
    <%= render "shared/games_card", { game: game } %>
  <% end %>
  </div>
</div>

<%
=begin %>

<img <%= image_tag(@game.photo) %> >

<%
=end %>
